version: 2.1

jobs:
  run_tests:
    docker:
      - image: circleci/node:12
    steps:
      - checkout
      - run:
          name: Install npm dependencies
          command: |
            sudo npm install -g
       - run:
          name: Install Jest Reporter
          command: npm install jest-html-reporters --save-dev
      - run:
          name: Run Tests
          command: |
<<<<<<< HEAD
              mkdir -p ~/test-results/reporter
              npm test --ci --reporters='jest-html-reporters'
=======
            jest --ci --runInBand --reporters=default --reporters='jest-html-reporters'
>>>>>>> d8752a23bb7f7039a29725ebcd229986ca10f232
 

workflows:
  build_test:
    jobs:
      - run_tests
